NODE_ENV=local
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/mydb
PORT=3000

# Secreto JWT (mín. 32 caracteres). Generar uno fuerte con:
#   openssl rand -base64 48
JWT_SECRET=changeme_generate_a_strong_secret_min_32_chars
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES_DAYS=7

API_URL=http://localhost:3000/api

# Orígenes permitidos por CORS, separados por comas. "*" permite todos (solo local).
CORS_ORIGINS=*
